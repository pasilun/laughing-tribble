# Feature: BuildingModel Tool Extraction

## ID
`building-model`

## Status
`superseded`

> **Superseded — too large for one dev cycle (failed verification 5/5).**
> Split into three loop-sized capabilities, built in order:
> 1. [[building-model-extraction]] — chat tool-calls maintain a structured
>    model, proven via a visible raw model echo (no formatted panel, no
>    computed values)
> 2. [[building-model-panel]] — a formatted summary panel (labelled fields,
>    "–" for unset, live update without reload)
> 3. [[building-model-computed]] — derived BYA and nockhöjd shown
>
> Do not implement this file. The full original spec lives in git history
> (and in `009b-building-model-tools.md`'s history).
